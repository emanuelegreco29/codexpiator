{
  "description": "Checks for a newer Codexpiator release on session start (never blocks startup), and deterministically blocks any git commit/PR command that includes AI/assistant attribution (Co-Authored-By Claude/Anthropic, a Claude Code footer, etc.) regardless of what a skill's prose says - prose alone can't reliably override a harness's own default attribution injection.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/check-update.sh",
            "timeout": 8
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/block-ai-attribution.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
